<pnp xmlns="urn:cisco:pnp" version="1.0" udi="{{ udi }}">
		<request xmlns="urn:cisco:pnp:backoff" correlator="{{ correlator }}">
				<backoff>
						<reason>image upgrade has finished</reason>
						<callbackAfter>
								<hours>{{ hours }}</hours>
								<minutes>{{ minutes }}</minutes>
								<seconds>{{ seconds }}</seconds>
						</callbackAfter>
				</backoff>
		</request>    
</pnp>